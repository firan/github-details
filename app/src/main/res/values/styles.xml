<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/PrimaryInteractionColor</item>
        <item name="colorPrimaryDark">@color/PrimaryInteractionColorDark</item>
        <item name="colorAccent">@color/PrimaryInteractionColor</item>
    </style>

    <style name="BottomNavigation">
        <item name="android:background">@color/BackgroundColor</item>
        <item name="itemIconTint">@color/bottom_navigation_item_color</item>
        <item name="itemTextColor">@color/bottom_navigation_item_color</item>
    </style>

    <style name="BaseTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/TabText</item>
        <item name="android:background">@color/PrimaryInteractionColor</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="tabTextColor">#82c8cdd2</item>
    </style>

    <style name="TabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TabLayout" parent="BaseTabLayout" />

</resources>
